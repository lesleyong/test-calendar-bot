version: '3.9'

services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - TZ=${TZ:-UTC}
      - DB_PATH=/app/data/bot.db
    volumes:
      - bot-data:/app/data

volumes:
  bot-data:
    driver: local
